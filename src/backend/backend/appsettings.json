{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "PostgresConnection": "Host=localhost;Port=5432;Database=moviemeetup;Username=postgres;Password=my_secure_password"
  },

  "Supabase": {
    "Url": "https://hzkpqfitsxnxcdefkqwt.supabase.co",

    "JwtSecret": "15a1a5d5-e111-49df-bf1a-4ea41cab1a3e",

    "ServiceRoleKey": ""
  },

  "Sentry": {
    "Dsn": ""
  },

  "Omdb": {
    "_comment": "OMDb API key comes from the Render env var Omdb__ApiKey (double underscore → maps to Omdb:ApiKey). Keep this blank — never commit a key here.",
    "ApiKey": ""
  },

  "CineMind": {
    "_comment": "Set both as Render env vars: CineMind__PuzzleSalt and CineMind__AdminSecret (double underscore). Keep both blank here. PuzzleSalt: without it the daily puzzle is still deterministic but becomes guessable in advance. AdminSecret: gates POST /api/game/catalog/seed (header x-admin-secret) — pick any random string, unrelated to the Apify secret used elsewhere in this app.",
    "PuzzleSalt": "",
    "AdminSecret": ""
  },

  "Apify": {
    "_comment": "Nightly CinemaClock showtime scrape. Both come from Render env vars Apify__ApiToken and Apify__WebhookSecret (double underscore → maps to Apify:ApiToken / Apify:WebhookSecret). Keep these blank — never commit them here. WebhookSecret must match the x-apify-secret header configured on the Apify webhook.",
    "ApiToken": "",
    "WebhookSecret": ""
  }
}
